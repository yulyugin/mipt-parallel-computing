% This file allows to produce either a separate PDF/PNG image
% See standalone documentation to understand underlying magic

\documentclass[tikz,convert={density=150,size=600,outext=.png}]{standalone}
\usetikzlibrary{shapes, calc, arrows, fit, positioning, decorations, patterns, decorations.pathreplacing, chains, snakes}
\input{../setup-web-fonts}
\input{../setup-packages}
\graphicspath{{../pictures/}} % path to pictures, trailing slash is mandatory.

% The actual drawing follows
\begin{document}

\begin{tikzpicture}[font=\footnotesize, >=latex]
	% five circles
	\foreach \A/\T in    { 90/{Получение инструкции},
						   18/{Декодирование},
						  306/{Считывание операндов},
						  234/{Выполнение},
						  162/{Запись результатов}
		} {
		\node[draw, circle, text width = 2.2cm, text badly centered](stage\A) at
            (\A:2.5cm) {\footnotesize\T};
		}
		% arrows, dumb way
		\draw[->, ] (stage90) -- (stage18);
		\draw[->, ] (stage18) -- (stage306);
		\draw[->, ] (stage306) -- (stage234);
		\draw[->, ] (stage234) -- (stage162);
		\draw[->, ] (stage162) -- (stage90);
\end{tikzpicture}

\end{document}
